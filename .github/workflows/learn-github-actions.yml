name: github-actions
run-name: is learning GitHub Actions
on: [push]
jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pwd
        pip install -r requirements.txt
    - run: python pytest Tests/APITests/test_user_service.py